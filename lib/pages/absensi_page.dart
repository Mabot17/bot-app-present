import 'package:flutter/material.dart';
import 'package:bot_app_present/pages/main_layout.dart';

class AbsensiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MainLayout(
      title: 'Absensi',
      body: Center(
        child: Text('Absensi Page'),
      ),
    );
  }
}
